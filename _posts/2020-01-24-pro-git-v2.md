---
layout: post
title: "[Pro Git] 1.3 시작하기 - Git 기초"
date: 2020-01-24 23:33:02 +0900
update: 2020-01-25 00:56:42 +0900
categories: ['git']
---

* TOC
{:toc}

# 추후 알아볼 것

* **TODO** [되돌리기](https://git-scm.com/book/ko/v2/ch00/_undoing)를 보면 Git에서 데이터를 어떻게 저장하고 손실을 어떻게 복구하는지 알 수 있다.

# 어떤 글을 쓸 것인가

[Pro Git 1.3 시작하기 - Git 기초](https://git-scm.com/book/ko/v2/%EC%8B%9C%EC%9E%91%ED%95%98%EA%B8%B0-Git-%EA%B8%B0%EC%B4%88)를 읽고 내가 이해한 만큼만 QNA 방식으로 정리한다.

# 본문의 구성 파악

## Q. Git의 핵심은 뭘까?

* Git이 무엇이고 어떻게 동작하는지 이해한다면 쉽게 Git을 효과적으로 사용할 수 있다.
* Git을 배우려면, 다른 VCS와 무엇이 다른지 이해해야 한다.

## Q. Git은 다른 VCS와 무엇이 다른가?

### A1. 차이가 아니라 스냅샷을 데이터로 다룬다

* 가장 큰 차이점은 데이터를 다루는 방법에 있다.
	* 다른 VCS: 파일의 변화를 데이터로 관리한다.
	* Git: 현재 파일이 존재하는 순간(=스냅샷)의 연속을 데이터로 관리한다.

다른 VCS에서는 파일에 변화가 생기지 않으면 기록하지 않는다. 반면에 Git은 파일이 존재하는 그 순간을 기록한다. 이때 Git은 파일이 달라지지 않았으면 성능을 위해 파일을 새로 저장하지 않고, 이전 상태의 파일에 대한 링크만 저장한다.

이 점 때문에 Git은 다른 시스템들이 과거로부터 답습해왔던 버전 컨트롤의 개념과 다르다는 것이고 많은 부분을 새로운 관점에서 바라본다.

### A2. 거의 모든 명령을 로컬에서 실행

프로젝트의 모든 히스토리가 로컬 디스크에 있기 때문에 모든 명령이 순식간에 실행된다.

### A3. Git의 무결성

Git은 데이터를 저장하기 전에 항상 체크섬을 구하고 그 체크섬으로 데이터를 관리한다.

> **체크섬**:  체크섬은 Git에서 사용하는 가장 기본적인(atomic) 데이터 단위이자 Git의 기본 철학이다. Git은 SHA-1 해시를 사용하여 체크섬을 만든다. 만든 체크섬은 40자 길이의 16진수 문자열이다. 파일의 내용이나 디렉토리 구조를 이용하여 체크섬을 구한다.

Git은 모든 것을 해시로 식별하고 실제로 파일을 이름으로 저장하지 않고 해당 파일의 해시로 저장한다.

> **TODO**: Git이 체크섬으로 데이터를 관리한다는 것과 무결성이 어떻게 연결되는 것이지?[^1]

### A4. Git은 데이터를 추가할 뿐

Git으로 무얼 하든 Git 데이터베이스에 데이터가 추가된다. 되돌리거나 데이터를 삭제할 방법이 없다. 다른 VCS처럼 Git도 커밋하지 않으면 변경사항을 잃어버릴 수 있다. 그러나 일단 스냅샷을 커밋하고 나면 데이터를 잃어버리기 어렵다.

> **TODO** [되돌리기](https://git-scm.com/book/ko/v2/ch00/_undoing)를 보면 Git에서 데이터를 어떻게 저장하고 손실을 어떻게 복구하는지 알 수 있다.

### A5. 세 가지 상태

Git은 파일을 `Committed`, `Modified`, `Staged` 이렇게 세 가지 상태로 관리한다.

* `Committed`: 데이터가 로컬 데이터베이스에 안전하게 저장됐다는 것을 의미한다.
* `Modified`: 수정한 파일을 아직 로컬 데이터베이스에 커밋하지 않은 것을 말한다.
* `Staged`: 현재 수정한 파일을 곧 커밋할 것이라고 표시한 상태를 의미한다.

> `git add [파일]` 명령어를 입력하면 해당 [파일]의 현재 스냅샷을 만들고, 이렇게 커밋할 스냅샷이 만들어진 파일의 상태를 `Staged`라고 부른다.

이 세 가지 상태는 Git 프로젝트의 세 가지 단계와 연결돼 있다. Git 디렉토리, 워킹 트리, Staging Area 이렇게 세 가지 단계가 있다.

* Git 디렉토리: Git이 프로젝트의 메타데이터와 객체 데이터베이스를 저장하는 곳을 말한다. 이 Git 디렉토리(`.git`)가 Git의 핵심이다.
* 워킹 트리: 프로젝트의 특정 버전을 checkout 한 것이다. Git 디렉토리는 지금 작업하는 디스크에 있고 그 디렉토리 안에 압축된 데이터베이스에서 파일을 가져와서 워킹 트리를 만든다.
* Staging Area: Git 디렉토리에 있다. 단순한 파일이고 곧 커밋할 파일에 대한 정보를 저장한다. Git에서는 기술 용어로 "Index"라고 하지만 Staging Area라고 불러도 상관 없다.

> **TODO**: 프로젝트의 메타데이터가 뭐지?[^2]

Git이 하는 일은 기본적으로 아래와 같다.

1. 워킹 트리에서 파일을 수정한다.
2. Staging Area에 파일을 Stage 해서 커밋할 스냅샷을 만든다. 모든 파일을 추가할 수도 있고 선택하여 추가할 수도 있다.
3. Staging Area에 있는 파일들을 커밋해서 Git 디렉토리에 영구적인 스냅샷으로 저장한다.

# 참조

* [Pro Git 1.3 시작하기 - Git 기초](https://git-scm.com/book/ko/v2/%EC%8B%9C%EC%9E%91%ED%95%98%EA%B8%B0-Git-%EA%B8%B0%EC%B4%88)

[^1]: 무결성이란 데이터베이스에 저장된 데이터 값과 그것이 표현하는 현실 세계의 실제값이 정확히 일치하는 성질을 의미한다. "Git이 데이터를 체크섬으로 관리한다는 것"과 "Git이 무결성을 가지고 있다는 것"은, Git의 특정 체크섬과 그 체크섬이 가리키는 데이터가 정확히 일치한다는 것을 의미한다.
[^2]: 저자나 커밋 메시지, 날짜 등을 의미한다.
